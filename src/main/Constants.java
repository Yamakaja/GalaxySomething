package main;

public final class Constants {
	
	// increases drawn images on screen / decreases area of images that is drawn
	// each by a tiny, tiny margin to prevent rendering artifacts (that would otherwise be delivered in Slick2D)
	public static final float RENDER_EPSILON = 0.08f;
	
	
	private Constants() {}
}
